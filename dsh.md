# DeepSeek Harness？

> @Author: 费加
> Update Time: 2026-08-20T20:05

## 一、DSH 与其他 Agent Harness 的主要区别

对于绝大部分常见的 Agent（例如 Claude Code、Codex、zcode、OpenCode），他们的都设计了一套 **不可修改** 的 *Agent Loop* 与 *上下文管理策略*。而 DeepSeek Harness 的核心设计就是 *一切皆插件*，这就意味着你可以 **自己设计与搭配** *Agent Loop* 和 *上下文管理策略* 乃至 **更复杂的自定义 Agent 工作流**，这就以为这 DSH 从架构设计上拥有 **极高的自由度和可能性**。

## 二、DSH *一切皆插件* 设计的优劣

### 1. 设计优势

DSH 的 *一切皆插件* 设计在理论上这是一个上线最高的设计，因为你可以根据 **不同的模型和任务** 设置不同的 *系统提示词* 以及 *工具* 和 *代理循环*，例如你能为前端任务配合最佳的工作流和模型以及相关工具，在每一个细节都能够做到对某个模型或任务调到最优。

### 2. 设计劣势

但这也意味着 DSH 的深入学习难度相比于普通 Agent 大很多，别的 Agent 可能顶多也就调一些 Skill、MCP、User Prompt、RAG；想要精通这就没有难度；但是在 DSH 你需要了解 Agent 设计的核心区域，需要了解 Agent Loop、Context Enginerring、Prompt Enginerring 等等。

## 三、DSH 的 *模式*

DSH 的 *模式*，说白了就是 **不同的插件组合**。例如你能创建一个 *前端模式*，专门加一些特定开发插件。

## 四、DSH 的插件框架：Cordis

[Cordis](https://github.com/cordiverse/cordis) 是一个使用 TypeScript 语言的 *时空可组合性（Spatiotemporal Composability）元框架*（反正他们的 [论文](https://github.com/cordiverse/paper) 是这样写的... 这东西还有论文？），**简而言之就是 DSH 的插件加/卸载器**

> 它想要解决的事情就是：**让插件能够动态卸载**

*这会涉及到：*
- 插件注册的事件监听器怎么办？
- 它创建的定时器和异步任务怎么办？
- 它提供的服务被其他插件依赖时怎么办？
- 它卸载之后，其他插件是否需要跟着更新或停止？

Cordis 试图把这些问题统一纳入运行时管理。插件可以声明自己依赖哪些服务，也可以注册事件、服务以及其他运行时副作用；当插件所属的生命周期结束时，相关资源理论上也可以被统一清理。

它所谓的“时空可组合性”，可以粗略理解成两个方向：

### 1. 时间可组合性

> **插件能否在运行时被移除后，撤销它产生的副作用**

比如一个插件注册了事件监听器、启动了定时器，或者创建了某些运行时服务。那么插件卸载之后，这些东西也应该能够被清理，而不是继续留在系统里。

### 2. 空间可组合性

> **可以理解成插件之间的依赖关系管理**

它的核心对象是 `Context`。插件通常通过 `ctx` 来访问服务、注册事件、声明依赖、创建副作用以及加载其他插件。每个插件运行时产生的资源，也可以绑定到对应的生命周期作用域中。

插件被卸载时，Cordis 主要能够处理的是运行时资源，例如事件监听器、定时器、服务注册和可管理的异步任务。**已经发送给模型的消息、模型已经生成的输出、已经写入磁盘的文件，通常不可能因为插件卸载就自动恢复。**

我目前还没有完全搞清楚 Cordis 在 DSH 中具体负责到哪一层。比如：

- Prompt 插件卸载后，已经加入的系统提示词会不会被移除？
- 多个 Prompt 插件之间是简单拼接，还是有优先级和作用域？
- Agent Loop 插件是替换主循环，还是以 middleware 的方式插入？
- 一个插件卸载后，依赖它的插件会暂停、重载，还是直接退出？

这些问题可能已经有部分实现，但目前公开文档还不足以让我建立一个完整、确定的模型。

> 我其实不是很能理解梁子到底要干什么... 因为对于普通的 Coding Agent 来说，会话生命周期通常就是任务始终（例如优化前端网页），插件似乎没太大必要中途卸载。为了切换一个模式，直接重新创建一个 Agent 也许就够了。

> 但如果 DSH 不只是要做一个传统的 Coding Agent，而是一个很超前的存在... 那么 Cordis 的生命周期和依赖管理就可能很有价值。

## 五、现阶段 DSH 面临的问题

### 1. 社区劣势

最近你可能听到 *“DSH 发布一个晚上就多了几百个插件”*，但是你能在 DSH 页面找到的基本是默认自带的，并且 **没有自述文件**，**没有标签**。你没办法说，这个官方提示词不好，我要换个 Sonnet 4.6 的提示词用。因为你根本 **不知道在哪里找** 以及这个插件 **会不会和其他插件发生冲突**。

### 2. 插件在 Agent 中的生命周期与冲突规范

目前我比较困惑的是，DSH 到底如何定义不同类型插件的组合规则。

从 Cordis 的设计来看，插件可以通过 `inject` 声明服务依赖，通过事件监听器参与运行流程，并且把注册的服务、事件和其他副作用绑定到自己的生命周期。因此，插件加载顺序、服务缺失和卸载后的资源清理，应该有比较明确的运行时机制。

但这还不能完全解决 Agent 层面的插件冲突。

例如：

- 两个 Prompt 插件的指令互相矛盾怎么办？
- 两个插件都注册同名工具时，谁覆盖谁？
- 两个插件监听同一个事件时，执行顺序是什么？
- 一个插件想让 Agent 重试，另一个插件想让 Agent 立即退出怎么办？
- 多个 Agent Loop 插件能否同时存在？
- 一个插件具体在 `agent/pre-step`、工具执行前，还是整个 turn 结束时运行？

DSH 的事件系统似乎已经区分了广播、串行、并行和 waterfall 等不同调度方式，但这些机制主要解决的是“代码如何执行”，不一定能解决“多个 Agent 策略如何组合”的问题。

因此，对于用户来说，一个插件至少应该公开以下信息：

- 它依赖哪些服务；
- 它注册或修改哪些服务；
- 它监听哪些事件；
- 它会在什么阶段运行；
- 它是否会修改 Prompt、工具或模型请求；
- 它是否会拦截或终止后续流程；
- 它与哪些插件不兼容；
- 它卸载时会清理哪些状态。

> 如果这些信息只能通过阅读源码才能确认，那么 DSH 的插件系统虽然拥有很高的自由度，但可预测性和可调试性仍然不足。